Redis Caching Backend (Node.js + MongoDB)
A backend project demonstrating industry-style Redis caching with MongoDB using the cache-aside pattern.
Built to understand how real backend systems improve performance and maintain data consistency.

🧠 What this project does

1.Stores user data in MongoDB (source of truth)

2.Uses Redis to cache read-heavy API responses

3.Implements cache hit / cache miss logic

4.Handles cache invalidation on update and delete

5.Tested end-to-end using Postman and Redis tools

